"""Pulse amplitude -> energy: templates, optimal filtering and the muon-line fit.

Depends on `common` and on `preprocessing` (it re-runs the shared triage classification
in memory to drop NOISE/PILEUP/clipped events from its noise and template models).
"""
